# Change list — feat/razorpay-revenue-e2e

Aligned to production columns:
- sv_payments.provider_order_id / provider_payment_id
- sv_payment_webhook_events.provider_event_id, event_id, event_name, payload_hash, payment_id, received_at, processed_at

WAIT: not merged. Live ₹1 + rows required before green.
