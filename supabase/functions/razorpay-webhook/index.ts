// Canonical Razorpay webhook handler source placeholder.
// The production function is deployed to Supabase; keep this file as the
// repository-side contract marker until the full generated function source
// is reconciled into version-controlled migrations/functions.
export {};
